{
  "name": "web-sayt",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist -p $PORT"
  },
  "devDependencies": {
    "firebase-tools": "^15.13.0",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "serve": "^14.2.6"
  }
}
